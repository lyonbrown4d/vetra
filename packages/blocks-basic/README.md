# @vetra/blocks-basic

Basic block definitions and renderer bindings for Vetra.

The root entry exports block constructors and types. The `@vetra/blocks-basic/react` subpath exports React block plugins for the official renderer.
